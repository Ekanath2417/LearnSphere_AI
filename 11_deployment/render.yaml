services:
  - type: web
    name: learnsphere-ai
    env: python
    buildCommand: pip install -r 06_code/requirements.txt
    startCommand: cd 06_code && gunicorn --bind 0.0.0.0:$PORT backend.wsgi:app
    healthCheckPath: /
    envVars:
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: FLASK_DEBUG
        value: "0"
